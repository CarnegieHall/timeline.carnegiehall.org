import { NumberLabel, NumberLabelProps } from '.';

export default {
  title: 'Components/NumberLabel',
  component: NumberLabel
};

export const Default = (props: NumberLabelProps) => (
  <NumberLabel {...props} />
);